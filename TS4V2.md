# ТЗ: «Посредник 2.0» — выгрузка контекста конфигурации 1С для LLM

> Внешняя обработка 1С:Предприятие 8.3, которая собирает из каталога выгруженной в XML конфигурации структурированный контекст (метаданные + исходники модулей + граф зависимостей) и отдаёт его в виде markdown-файлов, готовых к вставке в чат с LLM.

---

## 1. Контекст и предпосылки

### 1.1. Что есть сейчас

Существующая обработка `Посредник.epf` (одна форма «Костыль», ~17 КБ кода модуля) умеет одно: пройти каталог выгрузки конфигурации (`CommonModules/`, `Catalogs/`, `Documents/` и т.д.), распарсить корневые `.xml`-файлы метаданных через `ЧтениеXML` и собрать markdown-карту с реквизитами объектов.

### 1.2. Что не работает

| # | Баг | Причина |
|---|---|---|
| **B1** | **Не видит модули и формы** — только метаданные | Парсится только `*.xml` корня объекта. Папки `Ext/` и `Forms/` с `.bsl`-файлами не обходятся. |
| **B2** | Не различает составные типы и ссылки | XML читается через `ТипУзлаXML.НачалоЭлемента` без захода в `Type/TypeId` и `Type/QualifierName` — теряются `СправочникСсылка.X`, составные типы, длина/точность. |
| **B3** | Нет реквизитов табличных частей | Парсер ловит `Attribute`, `TabularSection`, `Dimension`, `Resource`, `StandardAttribute` как плоский список, без вложенности «таб.часть → её реквизиты». |
| **B4** | Нет связи метаданные ↔ исходники | Карта говорит «у справочника есть форма ФормаЭлемента», но не показывает, что в её модуле лежит. |
| **B5** | Нет графа вызовов | Невозможно ответить «кто использует этот общий модуль» без полнотекстового поиска руками. |
| **B6** | Нет протокола `FETCH`/`SCHEMA` | Это главная фича из инструкции для LLM — а обработка не умеет читать запрос и отдавать целевой кусок. |
| **B7** | UTF-8 без BOM / CRLF в результирующем файле не гарантированы | `ЗаписьТекста(..., КодировкаТекста.UTF8)` пишет с BOM по умолчанию — это ок, но не задокументировано. |
| **B8** | Кириллические пути и пробелы в путях не тестировались | Используется конкатенация `КореньКаталога + "\" + ТипПапки` — на сетевых путях и UNC может ломаться. |

### 1.3. Цель v2

Заменить «Посредник» обработкой, которая:

1. Видит **всё** — XML метаданных + `.bsl` модулей + формы.
2. Реализует протокол **`FETCH:`** и **`SCHEMA:`** из `1C_INSTRUCTIONS.md` буквально, строка-в-строку.
3. Дополнительно умеет:
   - **`MAP`** — карта конфигурации (`CONFIG_MAP.md`) с пометками `М/РС/Ф/К`.
   - **`GREP:строка`** — полнотекстовый поиск по всем `.bsl` с контекстом.
   - **`DEPS:Тип.Имя`** — кто вызывает указанный объект и кого вызывает он.
   - **`BUNDLE`** — экспорт всей конфигурации в один markdown/zip для подачи LLM целиком.

---

## 2. Глоссарий

| Термин | Значение |
|---|---|
| **Корень** | Каталог, в который из Конфигуратора выгружены файлы конфигурации (содержит `Configuration.xml`, `CommonModules/`, `Catalogs/`, …). |
| **Тип объекта** | Один из `CommonModules`, `Catalogs`, `Documents`, `Reports`, `DataProcessors`, `InformationRegisters`, `AccumulationRegisters`, `BusinessProcesses`, `Tasks`, `WebServices`, `CommonForms`, `Enums`, `ChartsOfCharacteristicTypes`, `ChartsOfAccounts`, `ChartsOfCalculationTypes`, `ExchangePlans`. |
| **Объект** | Конкретная единица метаданных, например `Catalogs/Контрагенты/`. |
| **Модуль** | Файл `.bsl` внутри объекта: `module`, `manager`, `object`, `recordset`, `form:Имя`, `command:Имя`. |
| **Запрос** | Строка вида `FETCH:...`, `SCHEMA:...`, `MAP`, `GREP:...`, `DEPS:...`, `BUNDLE`. |
| **Результат** | Файл `_CONTEXT.md` (или `CONFIG_MAP.md` для `MAP`, `BUNDLE.md` для `BUNDLE`). |

---

## 3. Функциональные требования

### 3.1. Форма

Одна управляемая форма со следующими элементами:

| Реквизит | Тип | Назначение |
|---|---|---|
| `КореньКонфигурации` | Строка | Путь к выгрузке. Сохраняется между запусками через `ХранилищеНастроекДанныхФормы`. |
| `СтрокаЗапроса` | Строка (многострочная) | Текст запроса. Можно вставить несколько строк — выполняются последовательно, дописываются в один `_CONTEXT.md`. |
| `ДописыватьВРезультат` | Булево | Если `Истина` — следующий запрос дописывается в существующий `_CONTEXT.md`; иначе перезаписывает. По умолчанию `Истина`. |
| `ПутьВыходногоФайла` | Строка | Путь к `_CONTEXT.md`. По умолчанию рядом с обработкой. |
| `ПротоколВыполнения` | Строка (многострочная, readonly) | Лог: что нашлось, что не нашлось, размеры. |

**Команды:**

| Команда | Действие |
|---|---|
| **Выполнить** | Парсит `СтрокаЗапроса`, выполняет, пишет результат. |
| **Открыть результат** | Открывает `_CONTEXT.md` в ассоциированном приложении. |
| **Сгенерировать карту** | Шорткат к `MAP` — пишет `CONFIG_MAP.md`. |
| **Очистить результат** | Удаляет `_CONTEXT.md`. |

### 3.2. Протокол запросов (полная сетка)

#### 3.2.1. `FETCH` — получить код модулей

**Синтаксис:**

```
FETCH:Тип.Имя.модуль[,модуль...];Тип.Имя.модуль[,модуль...]
```

Разделитель между объектами — `;`, между модулями одного объекта — `,`.

**Ключи модулей:**

| Ключ | Путь относительно объекта | Применимость |
|---|---|---|
| `module` | `Ext/Module.bsl` | `CommonModules`, `WebServices`, `CommonForms` |
| `manager` | `Ext/ManagerModule.bsl` | `Catalogs`, `Documents`, `Reports`, регистры, `Enums`, планы видов характеристик/счетов/расчёта |
| `object` | `Ext/ObjectModule.bsl` | `Catalogs`, `Documents`, `Reports`, `DataProcessors`, `BusinessProcesses`, `Tasks` |
| `recordset` | `Ext/RecordSetModule.bsl` | `InformationRegisters`, `AccumulationRegisters` |
| `form:Имя` | `Forms/Имя/Ext/Form/Module.bsl` | Любой объект с формами |
| `command:Имя` | `Commands/Имя/Ext/CommandModule.bsl` | Любой объект с командами |
| `all` | все существующие модули данного объекта | Любой |

**Поведение:**

- Если ключ-модуль не существует физически — в лог пишется `[skip] Catalogs.Контрагенты.recordset — нет файла`, в `_CONTEXT.md` ничего не добавляется.
- `all` раскрывается в фактический список модулей объекта, найденных в файловой системе (включая все формы и все команды).
- Перед каждым модулем в `_CONTEXT.md` пишется заголовок:

  ```markdown
  ### `Catalogs.Контрагенты` → `Ext/ObjectModule.bsl`
  ```text:bsl
  …содержимое…
  ```
  ```

- Кодировка чтения `.bsl` — UTF-8 с BOM (стандарт выгрузки Конфигуратора 1С). Если BOM нет — читать как UTF-8.

#### 3.2.2. `SCHEMA` — структура объекта без кода

**Синтаксис:**

```
SCHEMA:Тип.Имя;Тип.Имя
```

**Что извлекается:**

- Имя объекта, синоним, комментарий.
- **Стандартные реквизиты** (`StandardAttribute`) с типом.
- **Реквизиты** (`Attribute`) с полным типом: для составных — список всех типов, для строк — длина и допустимая длина, для чисел — точность/масштаб, для ссылок — на какой справочник/документ ссылается.
- **Табличные части** (`TabularSection`) с вложенными реквизитами — иерархически.
- **Измерения / ресурсы** (`Dimension`, `Resource`) для регистров.
- **Реквизиты адресации** (`AddressingAttribute`) для задач.
- **Формы** — список имён + тип формы (`ОсновнаяФорма`, `ФормаВыбора`, `ФормаСписка`…).
- **Команды** — список имён + действие.
- **Подсистемы** — в каких подсистемах объект участвует (для контекста).

**Формат вывода (markdown-таблицы):**

```markdown
## `Catalogs.Контрагенты`

**Синоним:** Контрагенты  
**Иерархия:** ИерархияЭлементов  
**Длина кода:** 11  
**Длина наименования:** 150

### Реквизиты

| Имя | Тип | Длина/Доп. |
|---|---|---|
| ИНН | Строка | 12 |
| ВидКонтрагента | ПеречислениеСсылка.ВидыКонтрагентов | — |
| ОсновнойМенеджер | СправочникСсылка.Пользователи | — |
| Адрес | Строка / СправочникСсылка.КонтактнаяИнформация | составной |

### Табличная часть `КонтактныеЛица`

| Имя | Тип |
|---|---|
| ФИО | Строка(150) |
| Телефон | Строка(50) |

### Формы

- `ФормаЭлемента` (ФормаОбъекта)
- `ФормаСписка` (ФормаСписка)
- `ФормаВыбора` (ФормаВыбора)

### Команды

- `СоздатьПоИНН`
```

#### 3.2.3. `MAP` — карта конфигурации

**Синтаксис:** `MAP` (без параметров).

**Вывод:** отдельный файл `CONFIG_MAP.md` рядом с `_CONTEXT.md`. Содержит **все** объекты конфигурации, сгруппированные по типу, с пометками:

| Метка | Значение |
|---|---|
| `М` | есть `ManagerModule.bsl` или `ObjectModule.bsl` |
| `РС` | есть `RecordSetModule.bsl` |
| `Ф:N` | N форм |
| `К:N` | N команд |

**Формат:**

```markdown
# CONFIG_MAP

> Корень: `D:\Configs\УТ\src`  
> Сгенерировано: 11.05.2026 14:23  
> Всего объектов: 1247

## CommonModules (89)

- `ОбщегоНазначения` М
- `ОбщегоНазначенияКлиент` М
- `РаботаСФайлами` М
- …

## Catalogs (156)

- `Контрагенты` М Ф:3 К:1
- `Номенклатура` М Ф:4 К:2
- …

## Documents (87)

- `РеализацияТоваровУслуг` М Ф:2
- …
```

Дополнительно — секция **Удалённые** в конце для объектов с префиксом `Удалить`, чтобы LLM их в навигации не использовал.

#### 3.2.4. `GREP` — полнотекстовый поиск по `.bsl`

**Синтаксис:**

```
GREP:строка_поиска
GREP:/регэксп/
GREP:строка_поиска?ignore_case=1&context=2&scope=CommonModules,Catalogs
```

**Параметры (после `?`):**

| Параметр | Значение по умолчанию | Описание |
|---|---|---|
| `ignore_case` | `1` | Регистронезависимый поиск |
| `context` | `2` | Сколько строк контекста до/после совпадения |
| `scope` | все типы | Список типов через запятую |
| `max_hits` | `200` | Лимит совпадений (защита от завала контекста) |

**Формат вывода:**

```markdown
## GREP: `НайтиПоНаименованию`

Найдено: 17 совпадений в 9 файлах.

### `CommonModules/РаботаСКонтрагентами/Ext/Module.bsl:142`

```text:bsl
140|     Если ПустаяСтрока(Наименование) Тогда Возврат Неопределено; КонецЕсли;
141| 
142|     Контрагент = Справочники.Контрагенты.НайтиПоНаименованию(Наименование, Истина);
143|     Если Контрагент.Пустая() Тогда
144|         Возврат Неопределено;
```
```

#### 3.2.5. `DEPS` — граф зависимостей

**Синтаксис:**

```
DEPS:Тип.Имя
DEPS:Тип.Имя?depth=2&direction=both
```

**Параметры:**

| Параметр | По умолчанию | Описание |
|---|---|---|
| `direction` | `both` | `in` (кто вызывает), `out` (кого вызывает), `both` |
| `depth` | `1` | Глубина обхода. `1` — только прямые зависимости. |

**Что считается зависимостью:**

1. **Вызов общего модуля:** `ОбщийМодуль.Метод(...)` — паттерн `<Идентификатор>.<Идентификатор>(`, где первый — имя из `CommonModules/`.
2. **Менеджер объекта:** `Справочники.Имя.Метод(`, `Документы.Имя.Метод(`, `Регистры.Имя.Метод(`.
3. **Тип объекта:** `СправочникСсылка.Имя`, `ДокументСсылка.Имя`, `Тип("СправочникСсылка.Имя")` в строках.
4. **Подписки на события** (`SubscriptionsDND.xml` / `EventSubscriptions/`) — если объект упомянут как источник.

**Формат вывода:**

```markdown
## DEPS: `CommonModules.РаботаСКонтрагентами`

### Кого вызывает (out)

- `CommonModules.ОбщегоНазначения` — 12 вызовов
  - `ОбщегоНазначения.СообщитьПользователю` (8×)
  - `ОбщегоНазначения.ЗначениеРеквизитаОбъекта` (4×)
- `Catalogs.Контрагенты` — 5 вызовов (через `Справочники.Контрагенты.*`)

### Кто вызывает (in)

- `Documents.РеализацияТоваровУслуг.ObjectModule` — 3 вызова
- `CommonForms.ПомощникСозданияКонтрагента.Form` — 1 вызов
- `CommonModules.ОбменДанными` — 7 вызовов
```

**Оговорка по точности:** анализ статический, текстовый. Динамические вызовы через `Выполнить()` / `Вычислить()` не отлавливаются — это пишется в `_CONTEXT.md` отдельной строкой при обнаружении.

#### 3.2.6. `BUNDLE` — экспорт всей конфигурации одним пакетом

**Синтаксис:**

```
BUNDLE
BUNDLE?include=CommonModules,Catalogs&exclude_prefix=Удалить&max_bytes=4000000
```

**Параметры:**

| Параметр | По умолчанию | Описание |
|---|---|---|
| `include` | все типы | Какие типы включать |
| `exclude_prefix` | `Удалить` | Пропустить объекты с этим префиксом |
| `max_bytes` | `0` (без лимита) | Лимит итогового файла в байтах. При превышении — разрезает на части `BUNDLE_001.md`, `BUNDLE_002.md`. |
| `format` | `md` | `md` — один markdown; `zip` — zip-архив с раздельными `.md` по объектам. |

**Назначение:** один раз снять «снимок» всей конфигурации для подачи в долгий контекст LLM, чтобы потом не запрашивать `FETCH` по 50 раз.

---

## 4. Архитектура

### 4.1. Модули обработки

```
ВнешняяОбработкаПосредник
├── Forms/
│   └── Основная/
│       └── Module.bsl              ← UI, кнопки, диспетчер запросов
└── Ext/
    └── ObjectModule.bsl            ← (пусто)
```

Логика разнесена по **общим модулям внутри обработки** (для внешней обработки — отдельные модули недоступны, поэтому всё внутри модуля формы плюс модуля объекта обработки). Стиль:

| Логический модуль | Назначение | Где физически |
|---|---|---|
| `ДиспетчерЗапросов` | Парсит `СтрокаЗапроса`, маршрутизирует | Модуль формы |
| `ЧтениеМетаданных` | XML → структура объекта | Модуль объекта обработки |
| `ЧтениеИсходников` | Файловая система → `.bsl` | Модуль объекта обработки |
| `АнализЗависимостей` | Текстовый анализ `.bsl` | Модуль объекта обработки |
| `ФорматированиеMD` | Сборка markdown | Модуль объекта обработки |
| `Утилиты` | Пути, кодировки, экранирование | Модуль объекта обработки |

В реальной реализации это **области** (`#Область … #КонецОбласти`) внутри модулей.

### 4.2. Внутренние структуры данных

```bsl
// Объект конфигурации — единое представление, заполняется один раз
СтруктураОбъекта = Новый Структура(
    "Тип, Имя, ПолноеИмя, ПутьНаДиске,
    | Синоним, Комментарий,
    | СтандартныеРеквизиты,   // Массив структур {Имя, Тип, Синоним}
    | Реквизиты,              // Массив структур {Имя, Тип, Длина, Точность, Синоним}
    | ТабличныеЧасти,         // Массив структур {Имя, Синоним, Реквизиты: Массив}
    | Измерения,              // для регистров
    | Ресурсы,                // для регистров
    | Формы,                  // Массив структур {Имя, Тип, ПутьМодуля}
    | Команды,                // Массив структур {Имя, ПутьМодуля}
    | Подсистемы,             // Массив строк
    | ЕстьManagerModule,
    | ЕстьObjectModule,
    | ЕстьRecordSetModule"
);
```

### 4.3. Парсер XML (исправление B2, B3)

**Используется `DOM` (`ПостроительDOM` + `ЧтениеXML`)**, а не построчное `Прочитать()`. Это решает баги составных типов и вложенности табличных частей.

**Псевдокод:**

```bsl
&НаСервере
Функция ПрочитатьXMLМетаданных(ПутьКФайлу)
    
    ЧтениеXML = Новый ЧтениеXML;
    ЧтениеXML.ОткрытьФайл(ПутьКФайлу);
    
    Построитель = Новый ПостроительDOM;
    Документ = Построитель.Прочитать(ЧтениеXML);
    ЧтениеXML.Закрыть();
    
    // Корневой узел — например, MetaDataObject → Catalog → Properties / ChildObjects
    КорневойУзел = Документ.ПолучитьЭлементыПоИмени("Catalog").Элемент(0);
    Если КорневойУзел = Неопределено Тогда
        // Попробовать другие типы: Document, InformationRegister...
        ...
    КонецЕсли;
    
    Результат = ПустаяСтруктураОбъекта();
    
    ЗаполнитьСвойства(Результат, КорневойУзел);          // Имя, Синоним, Комментарий
    ЗаполнитьСтандартныеРеквизиты(Результат, КорневойУзел);
    ЗаполнитьРеквизиты(Результат, КорневойУзел);          // включая составные типы
    ЗаполнитьТабличныеЧасти(Результат, КорневойУзел);     // с вложенностью
    ЗаполнитьИзмеренияРесурсы(Результат, КорневойУзел);
    
    Возврат Результат;
    
КонецФункции
```

**Детали по составным типам:**

```xml
<Properties>
  <Attribute>
    <Name>ВидКонтрагента</Name>
    <Type>
      <Type>cfg:EnumRef.ВидыКонтрагентов</Type>
    </Type>
    <Synonym><v8:item><v8:lang>ru</v8:lang><v8:content>Вид</v8:content></v8:item></Synonym>
  </Attribute>
  <Attribute>
    <Name>ПлательщикИлиПолучатель</Name>
    <Type>
      <Type>cfg:CatalogRef.Контрагенты</Type>
      <Type>xs:string</Type>
      <StringQualifiers>
        <Length>100</Length>
      </StringQualifiers>
    </Type>
  </Attribute>
</Properties>
```

Парсер обходит **все** `<Type>` внутри обёртки `<Type>` и собирает их в массив. Префиксы (`cfg:CatalogRef`, `xs:string`, `v8:Date` и т.п.) маппятся на человеческие имена через словарь:

```bsl
Словарь.Вставить("cfg:CatalogRef", "СправочникСсылка");
Словарь.Вставить("cfg:DocumentRef", "ДокументСсылка");
Словарь.Вставить("cfg:EnumRef", "ПеречислениеСсылка");
Словарь.Вставить("xs:string", "Строка");
Словарь.Вставить("xs:decimal", "Число");
Словарь.Вставить("xs:boolean", "Булево");
Словарь.Вставить("xs:dateTime", "Дата");
// ... полный список ниже в Приложении А
```

### 4.4. Чтение исходников (исправление B1)

**Главный баг старого «Посредника»**: он останавливался на XML. Новая версия:

1. Для каждого объекта строит **карту физических файлов модулей**:
   ```
   Catalogs/Контрагенты/Ext/ManagerModule.bsl
   Catalogs/Контрагенты/Ext/ObjectModule.bsl
   Catalogs/Контрагенты/Forms/ФормаЭлемента/Ext/Form/Module.bsl
   Catalogs/Контрагенты/Forms/ФормаСписка/Ext/Form/Module.bsl
   Catalogs/Контрагенты/Commands/СоздатьПоИНН/Ext/CommandModule.bsl
   ```
2. На каждый ключ запроса (`module`, `manager`, `object`, `recordset`, `form:X`, `command:X`, `all`) — резолвит соответствующий путь.
3. Читает файл через `ЧтениеТекста` с явной кодировкой `UTF8`. Проверяет наличие BOM.
4. Для `all` обходит `Forms/` и `Commands/` рекурсивно через `НайтиФайлы(..., "*", Истина)` с фильтрацией по имени файла модуля.

### 4.5. Граф зависимостей (новая фича)

**Двухпроходный анализ.**

**Проход 1: индекс публичных имён.**

При первом обращении к корню (или по команде `Переиндексировать`) обработка строит словари:

| Словарь | Ключ | Значение |
|---|---|---|
| `ОбщиеМодули` | имя общего модуля | путь к `Module.bsl` |
| `Менеджеры` | имя справочника/документа/... | тип + путь |
| `СсылочныеТипы` | `СправочникСсылка.X` | `Catalogs.X` |

Индекс кэшируется в `ХранилищеНастроекДанныхФормы` с ключом, основанным на хэше списка файлов корня (быстрая проверка изменений по mtime + размер). Полная переиндексация — `~5–15 сек` на конфигурацию из 1500 объектов.

**Проход 2: текстовый разбор `.bsl`.**

Регулярные выражения (через `СтрНайти` + ручной парсинг, т.к. в 1С нет regex до 8.3.23 — для совместимости делаем без них):

- Снять комментарии (`//` до конца строки, не внутри строкового литерала).
- Снять строковые литералы (между неэкранированными `"`).
- На оставшемся искать паттерны:
  - `<Идентификатор>.<Идентификатор>(` — если первый есть в `ОбщиеМодули` или `Менеджеры`, это вызов.
  - `Справочники.<Идентификатор>` / `Документы.<Идентификатор>` / `РегистрыСведений.<Идентификатор>` / `РегистрыНакопления.<Идентификатор>` — обращение к менеджеру.
  - `СправочникСсылка.<Идентификатор>` и аналоги — типовая зависимость (без вызова, но связь есть).

**В строковых литералах** (после снятия) — отдельный проход для `Тип("СправочникСсылка.X")` и для строк запроса:

```bsl
Запрос.Текст = "ВЫБРАТЬ … ИЗ Справочник.Контрагенты КАК …";
```

Здесь `Справочник.Контрагенты` (без «Ссылка») в контексте строки запроса = зависимость от `Catalogs.Контрагенты`. Помечается в результате как «(через запрос)».

**Динамический вызов** (`Выполнить()`, `Вычислить()`) детектируется и выводится отдельной строкой:

```markdown
⚠️ В модуле обнаружено 2 вызова `Выполнить()` — динамические зависимости не отслеживаются.
```

### 4.6. Запись результата

- Файл `_CONTEXT.md` пишется в **UTF-8 с BOM**, переводы строк — `Символы.ПС` (LF). Это компромисс между совместимостью с Windows-редакторами и markdown-парсерами.
- При `ДописыватьВРезультат = Истина` — `ЗаписьТекста(Путь, КодировкаТекста.UTF8, , Истина)` (последний параметр — `Добавление`).
- Перед каждым новым блоком пишется разделитель:
  ```markdown
  
  ---
  
  ```

### 4.7. Кеширование

| Что | Где | Ключ инвалидации |
|---|---|---|
| Индекс публичных имён | `ХранилищеНастроекДанныхФормы` | хэш `(путь_корня, общий_размер_всех_xml, max(mtime))` |
| Распарсенные XML объектов | `Кеш` (соответствие, переменная модуля формы, живёт в течение сессии) | путь + mtime файла |
| Содержимое `.bsl` | не кешируется | — |

---

## 5. Нефункциональные требования

| # | Требование |
|---|---|
| **NF1** | Совместимость: платформа **8.3.14+** (`ПостроительDOM`, `СтрНайти`, управляемые формы доступны). |
| **NF2** | Работа без подключения к информационной базе — обработка должна открываться как внешняя в режиме «Файл → Открыть» в Конфигураторе или в `1cv8.exe ENTERPRISE` поверх любой пустой ИБ. |
| **NF3** | Кириллица в путях, пробелы, UNC-пути (`\\server\share\config`) — должны работать. Тест-кейсы в §7. |
| **NF4** | Размер `_CONTEXT.md` не ограничен, но при `> 5 МБ` — предупреждение в логе («контекст вряд ли влезет в LLM, рассмотри BUNDLE с разбиением»). |
| **NF5** | Все файловые операции — `&НаСервере` (для внешней обработки в управляемом приложении — это серверная процедура обработки; клиентский код только для UI). |
| **NF6** | Никаких внешних компонент, COM, ADODB, WMI. Только средства встроенного языка 1С. |
| **NF7** | Время выполнения `FETCH` одного объекта — `< 200 мс` (без учёта первичной индексации). `MAP` на 1500 объектов — `< 30 сек`. `BUNDLE` — `< 2 мин`. |
| **NF8** | Все сообщения пользователю и в логе — на русском. Ключевые слова протокола (`FETCH`, `SCHEMA`, …) — латиницей, как в инструкции. |

---

## 6. API/протокол: формальная грамматика

```ebnf
запрос         = команда ( ";" команда )* ;

команда        = команда_fetch
               | команда_schema
               | команда_map
               | команда_grep
               | команда_deps
               | команда_bundle ;

команда_fetch  = "FETCH:" объект_модули ( ";" объект_модули )* ;
объект_модули  = тип "." имя "." модуль ( "," модуль )* ;

команда_schema = "SCHEMA:" тип "." имя ( ";" тип "." имя )* ;

команда_map    = "MAP" [ "?" параметры ] ;
команда_grep   = "GREP:" подстрока [ "?" параметры ] ;
команда_deps   = "DEPS:" тип "." имя [ "?" параметры ] ;
команда_bundle = "BUNDLE" [ "?" параметры ] ;

тип            = "CommonModules" | "Catalogs" | "Documents" 
               | "Reports" | "DataProcessors"
               | "InformationRegisters" | "AccumulationRegisters"
               | "BusinessProcesses" | "Tasks" | "WebServices"
               | "CommonForms" | "Enums"
               | "ChartsOfCharacteristicTypes" | "ChartsOfAccounts"
               | "ChartsOfCalculationTypes" | "ExchangePlans" ;

имя            = идентификатор_1С ;       // [A-Za-zА-Яа-я_][A-Za-zА-Яа-я_0-9]*

модуль         = "module" | "manager" | "object" | "recordset"
               | "form:" имя | "command:" имя | "all" ;

параметры      = пара ( "&" пара )* ;
пара           = ключ "=" значение ;
```

**Парсер запросов должен:**

1. Принимать многострочный ввод — каждая непустая строка = отдельный запрос (или несколько через `;`).
2. Игнорировать строки, начинающиеся с `#` (комментарии).
3. На синтаксической ошибке — писать в лог `[error] строка N: …` и продолжать со следующей строки.
4. Лог в конце сессии: «Выполнено успешно: X, с ошибками: Y, пропущено: Z».

---

## 7. Тест-кейсы

> Каждый тест-кейс — это пара (вход, ожидаемое поведение). Прогоняются на тестовой выгрузке.

### 7.1. Минимальный тестовый стенд

В `/tests/fixtures/` лежат две заранее выгруженные конфигурации:

| Стенд | Объектов | Особенности |
|---|---|---|
| `tiny` | 5 | Один справочник, один документ, один общий модуль, один регистр сведений. |
| `medium` | 150 | Кириллические имена, составные типы, табличные части с реквизитами-ссылками, удалённые объекты с префиксом `Удалить`. |

### 7.2. Тесты протокола

| # | Запрос | Ожидание |
|---|---|---|
| T1 | `FETCH:CommonModules.ОбщегоНазначения.module` | `_CONTEXT.md` содержит блок с заголовком и содержимым `Ext/Module.bsl`. |
| T2 | `FETCH:Catalogs.Контрагенты.manager,object` | Два блока подряд: ManagerModule и ObjectModule. |
| T3 | `FETCH:Catalogs.Контрагенты.all` | Блоки для всех найденных модулей объекта: manager, object, все формы, все команды. |
| T4 | `FETCH:Catalogs.НетТакого.manager` | В лог: `[skip] объект не найден`. В `_CONTEXT.md` — ничего. |
| T5 | `FETCH:Catalogs.Контрагенты.recordset` | В лог: `[skip] не применимо к типу Catalogs`. |
| T6 | `FETCH:Catalogs.Контрагенты.form:НесуществующаяФорма` | В лог: `[skip] форма НесуществующаяФорма не найдена`. |
| T7 | `SCHEMA:Catalogs.Номенклатура` | Markdown с таблицами реквизитов и хотя бы одной табличной части. Все типы — в человеческом виде (`СправочникСсылка.X`, а не `cfg:CatalogRef.X`). |
| T8 | `SCHEMA:Documents.РеализацияТоваров;InformationRegisters.ЦеныНоменклатуры` | Два раздела, у регистра отображены **Измерения** и **Ресурсы**. |
| T9 | `MAP` | Файл `CONFIG_MAP.md` создан, содержит все 150 объектов из `medium`, объекты с префиксом `Удалить` — в отдельной секции. |
| T10 | `GREP:НайтиПоНаименованию?context=1` | Все вхождения с 1 строкой контекста, не больше 200 hits. |
| T11 | `GREP:/Тип\("СправочникСсылка/` | Регулярное выражение работает (если поддерживается платформой) или возвращает понятную ошибку. |
| T12 | `DEPS:CommonModules.РаботаСКонтрагентами` | Список входящих и исходящих зависимостей. Вызовы через `Справочники.X` отнесены к `Catalogs.X`. |
| T13 | `DEPS:Catalogs.Контрагенты?direction=in` | Только входящие. |
| T14 | `BUNDLE?max_bytes=500000` | Создаются `BUNDLE_001.md`, `BUNDLE_002.md`, …; ни один не превышает 500 КБ. |
| T15 | `BUNDLE?include=CommonModules,Catalogs` | Только эти типы в результате. |

### 7.3. Тесты на баги старой версии

| # | Сценарий | Что было сломано | Что должно быть |
|---|---|---|---|
| B1-T | `SCHEMA` на объекте с табличной частью | ТЧ не отображалась или путалась с обычными реквизитами | ТЧ выводится отдельным подзаголовком с собственной таблицей реквизитов. |
| B2-T | Составной тип реквизита (`Строка / СправочникСсылка.X`) | Выводился один тип или префикс `cfg:` | Выводятся все типы через ` / `, без префиксов. |
| B2-T2 | Реквизит `Строка(150)` | Длина терялась | Выводится как `Строка(150)`. |
| B3-T | Реквизит табличной части — ссылка на справочник | Не определялась | Выводится `СправочникСсылка.X`. |
| B4-T | `FETCH:Catalogs.X.form:Y` | Не работал — папка `Forms` не обходилась | Работает. |
| B8-T | Корень — `D:\Конфиги\УТ 11\src` (кириллица + пробел) | Возможные сбои | Работает идентично латинскому пути. |

### 7.4. Тесты на ошибки

| # | Вход | Ожидание |
|---|---|---|
| E1 | Пустой `КореньКонфигурации` | Сообщение «Укажи путь к конфигурации», ничего не пишется. |
| E2 | Несуществующий путь | Сообщение «Каталог не найден: …», ничего не пишется. |
| E3 | Путь есть, но не похож на выгрузку (нет `Configuration.xml`) | Сообщение «По пути не найден `Configuration.xml` — это точно выгрузка конфигурации?». |
| E4 | Битый XML в одном из объектов | Объект пропускается, в лог: `[error] Catalogs.X — битый XML: <описание>`. Остальные обрабатываются. |
| E5 | Запрос `БРЕД:что-то` | `[error] неизвестная команда «БРЕД»`. |
| E6 | `FETCH` без `.модуль` | `[error] FETCH: ожидается «Тип.Имя.модуль»`. |

---

## 8. Поэтапная разработка

| Этап | Объём | Критерий готовности |
|---|---|---|
| **M1. Каркас** | Форма, поля, чтение/запись `_CONTEXT.md`, парсер строки запроса, диспетчер. | Запрос `FETCH:CommonModules.X.module` работает на стенде `tiny`. |
| **M2. FETCH полный** | Все ключи модулей, `all`, обход форм/команд. | Тесты T1–T6 зелёные. |
| **M3. SCHEMA через DOM** | Парсер XML, словарь типов, составные типы, табличные части, регистры. | Тесты T7–T8, B1-T, B2-T, B2-T2, B3-T зелёные. |
| **M4. MAP** | Обход всех типов, метки М/РС/Ф/К, секция «Удалённые». | Тест T9 зелёный. |
| **M5. GREP** | Полнотекстовый поиск, контекст, фильтры. | Тесты T10–T11 зелёные. |
| **M6. DEPS** | Индекс публичных имён, текстовый анализ, кеширование. | Тесты T12–T13 зелёные. |
| **M7. BUNDLE** | Сборка всего в один файл, разбиение по размеру, формат zip. | Тесты T14–T15 зелёные. |
| **M8. Тесты ошибок и кириллицы** | Обработка всех путей-ошибок, тест B8-T. | Тесты E1–E6, B8-T зелёные. |
| **M9. Документация** | `README.md` рядом с `.epf`, обновление `1C_INSTRUCTIONS.md` под новый протокол. | Файлы готовы. |

---

## Приложение А. Словарь типов XML → 1С

| XML | 1С |
|---|---|
| `xs:string` | `Строка` |
| `xs:decimal` | `Число` |
| `xs:boolean` | `Булево` |
| `xs:dateTime` | `Дата` |
| `xs:base64Binary` | `ХранилищеЗначения` |
| `v8:UUID` | `УникальныйИдентификатор` |
| `cfg:CatalogRef.X` | `СправочникСсылка.X` |
| `cfg:DocumentRef.X` | `ДокументСсылка.X` |
| `cfg:EnumRef.X` | `ПеречислениеСсылка.X` |
| `cfg:ChartOfCharacteristicTypesRef.X` | `ПланВидовХарактеристикСсылка.X` |
| `cfg:ChartOfAccountsRef.X` | `ПланСчетовСсылка.X` |
| `cfg:ChartOfCalculationTypesRef.X` | `ПланВидовРасчётаСсылка.X` |
| `cfg:BusinessProcessRef.X` | `БизнесПроцессСсылка.X` |
| `cfg:TaskRef.X` | `ЗадачаСсылка.X` |
| `cfg:ExchangePlanRef.X` | `ПланОбменаСсылка.X` |
| `cfg:DocumentObject.X` | `ДокументОбъект.X` |
| `cfg:CatalogObject.X` | `СправочникОбъект.X` |
| `cfg:AccumulationRegisterRecordSet.X` | `РегистрНакопленияНаборЗаписей.X` |
| `cfg:InformationRegisterRecordSet.X` | `РегистрСведенийНаборЗаписей.X` |

Список расширяется по мере обнаружения новых префиксов в реальных выгрузках.

---

## Приложение Б. Образец содержимого `_CONTEXT.md`

```markdown
# _CONTEXT

> Корень: `D:\Configs\УТ\src`  
> Сгенерировано: 11.05.2026 14:23:07

---

## SCHEMA: `Catalogs.Контрагенты`

**Синоним:** Контрагенты

### Реквизиты

| Имя | Тип | Длина/Доп. |
|---|---|---|
| ИНН | Строка | 12 |
| ВидКонтрагента | ПеречислениеСсылка.ВидыКонтрагентов | — |

### Табличная часть `КонтактныеЛица`

| Имя | Тип |
|---|---|
| ФИО | Строка(150) |
| Телефон | Строка(50) |

### Формы

- `ФормаЭлемента` (ФормаОбъекта)
- `ФормаСписка` (ФормаСписка)

---

## FETCH: `Catalogs.Контрагенты` → `Ext/ObjectModule.bsl`

```text:bsl
Процедура ПередЗаписью(Отказ) Экспорт
    …
КонецПроцедуры
```

---

## DEPS: `CommonModules.РаботаСКонтрагентами`

### Кого вызывает

- `CommonModules.ОбщегоНазначения` — 12 вызовов
- `Catalogs.Контрагенты` — 5 вызовов

### Кто вызывает

- `Documents.РеализацияТоваровУслуг.ObjectModule` — 3 вызова
```

---

## Приложение В. Что не входит в v2 (роадмап v3)

- Сравнение двух выгрузок (диф между версиями конфигурации).
- Экспорт ролей и прав.
- Поддержка расширений конфигурации (`*.cfe`).
- Парсинг СКД-схем отчётов.
- Веб-интерфейс / API-режим (запрос по HTTP).
- Интеграция с git: автокоммит `_CONTEXT.md` после каждого запроса.
