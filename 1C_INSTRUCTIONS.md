# Инструкции для работы с исходниками 1С:Предприятие 8.3

> Универсальный промпт для работы с любой конфигурацией.
> Вставляй в начало нового чата или добавь в **Project Instructions**.

---

## Стек

| Параметр | Значение |
|---|---|
| Платформа | 1С:Предприятие 8.3 |
| Язык | BSL (встроенный язык 1С) |
| Исходники | Выгружены из Конфигуратора в файловую систему |
| Корень конфигурации | `[ПУТЬ_К_КОНФИГУРАЦИИ]` ← уточняется в начале чата |

---

## Структура каталогов (стандарт 1С)

```
Корень\
├── CommonModules\
│   └── Имя\Ext\Module.bsl
├── CommonForms\
│   └── Имя\Ext\Form\Module.bsl
├── Catalogs\
│   └── Имя\
│       ├── Ext\ManagerModule.bsl
│       ├── Ext\ObjectModule.bsl
│       ├── Forms\ИмяФормы\Ext\Form\Module.bsl
│       └── Commands\ИмяКоманды\Ext\CommandModule.bsl
├── Documents\
│   └── Имя\
│       ├── Ext\ManagerModule.bsl
│       └── Ext\ObjectModule.bsl
├── InformationRegisters\
│   └── Имя\
│       ├── Ext\ManagerModule.bsl
│       └── Ext\RecordSetModule.bsl
├── AccumulationRegisters\
│   └── Имя\
│       ├── Ext\ManagerModule.bsl
│       └── Ext\RecordSetModule.bsl
├── DataProcessors\
│   └── Имя\Forms\ИмяФормы\Ext\Form\Module.bsl
├── Reports\
│   └── Имя\Ext\ObjectModule.bsl
├── BusinessProcesses\
│   └── Имя\Ext\ObjectModule.bsl
├── Tasks\
│   └── Имя\Ext\ObjectModule.bsl
└── WebServices\
    └── Имя\Ext\Module.bsl
```

---

## Протокол получения кода

Когда нужен реальный код или структура объекта — ИИ выдаёт строку запроса.
Пользователь вставляет её в обработку **Посредник**, запускает,
получает `.md`-файл и вставляет его содержимое в чат.

Имя результата собирается из самого запроса, поэтому по названию файла
сразу видно, что в нём лежит:

| Запрос | Файл результата |
|---|---|
| `SCHEMA:Catalogs.Контрагенты` | `Запрос_SCHEMA_Контрагенты.md` |
| `FETCH:Documents.РеализацияТоваров.object` | `Запрос_FETCH_РеализацияТоваров.md` |
| `SCHEMA:` по нескольким объектам | `Запрос_SCHEMA_A_B_C.md` |
| Больше трёх объектов | `Запрос_SCHEMA_A_B_C_и_ещё_5.md` |
| `FETCH:` и `SCHEMA:` в одном сеансе | `Запрос_FETCH-SCHEMA_ИмяОбъекта.md` |
| `SEARCH:ПодобратьДоговор` | `Запрос_SEARCH_ПодобратьДоговор.md` |
| `DEPENDENCIES:Catalogs.Контрагенты` | `Запрос_DEPS_Контрагенты.md` |
| `OUTLINE:CommonModules.РаботаСФайлами` | `Запрос_OUTLINE_РаботаСФайлами.md` |
| `PROC:CommonModules.РаботаСФайлами.ПрочитатьФайл` | `Запрос_PROC_ПрочитатьФайл.md` |
| `REGION:Documents.Реализация.object#ОбработчикиСобытий` | `Запрос_REGION_ОбработчикиСобытий.md` |
| `LINES:CommonModules.РаботаСФайлами.module:200-260` | `Запрос_LINES_РаботаСФайлами_200-260.md` |

Карта конфигурации сохраняется отдельно как `КартаКонфигурации_ГГГГ-ММ-ДД_ЧЧ-ММ-СС.md`.

### FETCH — получить код модулей

```
FETCH:Тип.ИмяОбъекта.модули;Тип.ИмяОбъекта.модули
```

#### Доступные модули

| Ключ | Файл | Применимость |
|---|---|---|
| `module` | `Ext\Module.bsl` | **CommonModules, WebServices** |
| `manager` | `Ext\ManagerModule.bsl` | Catalogs, Documents, Registers и др. |
| `object` | `Ext\ObjectModule.bsl` | Catalogs, Documents, BusinessProcesses и др. |
| `recordset` | `Ext\RecordSetModule.bsl` | InformationRegisters, AccumulationRegisters |
| `form:ИмяФормы` | `Forms\ИмяФормы\Ext\Form\Module.bsl` | Любые объекты с формами |
| `command:ИмяКоманды` | `Commands\ИмяКоманды\Ext\CommandModule.bsl` | Объекты с командами |
| `all` | все найденные модули объекта | Любые |

#### Примеры

```
FETCH:CommonModules.ОбщийМодуль.manager

FETCH:Catalogs.Контрагенты.manager,object

FETCH:Documents.РеализацияТоваров.object;CommonModules.ПроведениеДокументов.manager

FETCH:DataProcessors.ЗагрузкаДанных.form:ОсновнаяФорма

FETCH:Catalogs.Номенклатура.all
```

#### Когда FETCH не нужен

`FETCH` тянет модуль целиком. На модуле в 20 000 строк это съедает контекст
чата, после чего запросить соседний модуль уже нельзя. Если нужен не весь
модуль, а конкретный метод или кусок — маршрут `OUTLINE:` → `PROC:` / `LINES:`
(см. ниже) даёт то же самое за сотню строк.

---

### SCHEMA — получить структуру объекта

Реквизиты, измерения, ресурсы, табличные части, формы, команды — без кода.

```
SCHEMA:Тип.ИмяОбъекта;Тип.ИмяОбъекта
```

#### Что возвращает

Таблица из трёх колонок: **Реквизит**, **Тип**, **Раздел**.

```
| Реквизит | Тип | Раздел |
|---|---|---|
| `Ref` | СправочникСсылка.Контрагенты | ⚙ Станд. реквизит |
| `Code` | Строка(9) | ⚙ Станд. реквизит |
| `ИНН` | Строка(12, фикс) | ✏ Реквизит |
| `Сумма` | Число(15,2) | ✏ Реквизит |
| `ДатаСоздания` | Дата(дата) | ✏ Реквизит |
| `ДополнительныеРеквизиты` | ТабличнаяЧасть | ☰ Таб. часть |
| `ДополнительныеРеквизиты.LineNumber` | Число(9) | ⚙ Станд. реквизит |
| `ДополнительныеРеквизиты.Значение` | Характеристика.ДопРеквизиты | ✏ Реквизит |
| `ФормаЭлемента` | Форма | ▣ Форма |
| `ВебВерсияСПАРК` | Команда | ▶ Команда |
```

Особенности вывода:

- **Квалификаторы** идут прямо в типе: `Строка(150)`, `Строка(12, фикс)`,
  `Число(15,2)`, `Дата(дата)`, `Дата(дата и время)`.
  Строка без длины (`Строка` без скобок) — неограниченной длины.
- **Составные типы** перечисляются через запятую, у каждого свой квалификатор:
  `Строка(100), СправочникСсылка.ФизическиеЛица`.
- **Реквизиты табличных частей** идут с префиксом: `Отдыхающие.НомерПутевки`.
  Сама ТЧ выводится строкой выше своих реквизитов.
- **Стандартные реквизиты** типизированы: `Ref`, `Parent`, `Owner`, `Code`,
  `Description`, `Number`, `Date`, `Posted`, `DeletionMark`, `IsFolder`,
  `LineNumber`, `Period`, `Recorder`, `Active`, `RecordType`.
- **Методы HTTP-сервисов** идут с префиксом шаблона URL:
  `SetContractStatus.POST`.

#### Иконки разделов

| Иконка | Раздел | | Иконка | Раздел |
|---|---|---|---|---|
| ✏ | Реквизит | | ▣ | Форма |
| ◆ | Измерение | | ▶ | Команда |
| Σ | Ресурс | | ⇄ | Операция веб-сервиса |
| ⚙ | Станд. реквизит | | ◈ | Шаблон URL |
| ☰ | Таб. часть | | ⚒ | Метод |
| ⊕ | Общий реквизит | | | |

#### Примеры

```
SCHEMA:Catalogs.Номенклатура

SCHEMA:Documents.ЗаказПокупателя;InformationRegisters.ЦеныНоменклатуры

SCHEMA:AccumulationRegisters.ОстаткиТоваров

SCHEMA:HTTPServices.arsDirectumExchange
```

#### Чего SCHEMA не показывает

- `Код` и `Наименование` справочников отдельными строками — они лежат в XML
  как свойства объекта, а не как реквизиты (но их длины видны в типе
  стандартных реквизитов `Code` и `Description`).
- `SCHEMA:Constants.*` вернёт «Реквизиты не найдены» — у константы тип описан
  на верхнем уровне, вне контейнера реквизитов.

---

### SEARCH — найти, где что лежит

Полнотекстовый поиск по конфигурации. Нужен, когда неизвестно, в каком объекте
искать: `SEARCH` возвращает таблицы, последняя колонка которых — готовая строка
`FETCH:` или `SCHEMA:` для следующего шага.

```
SEARCH:текст|опция|опция
```

Разделитель опций — `|`, а не `:` или `;`, поэтому искомый текст может содержать
и то, и другое: `SEARCH:Отказ = Истина;|code` корректно ищет строку с точкой
с запятой.

#### Опции

| Опция | Что делает |
|---|---|
| `name` | только имена объектов |
| `code` | только содержимое BSL |
| `attr` | только реквизиты и типы |
| `all` | все три (так же, если режим не указан) |
| `max=N` | лимит результатов, по умолчанию 50 |
| `in=Тип,Тип` | ограничить типами объектов (папками выгрузки) |

Если указан хотя бы один из `name` / `code` / `attr`, остальные выключаются.

#### Что возвращает

До трёх таблиц — «Имена объектов», «Код», «Реквизиты и типы»:

```
| Объект | Модуль | Строки | Запрос |
|---|---|---|---|
| `Tasks.нмЗадача` | form:ФормаЗадачи | 42, 44, 46 | `FETCH:Tasks.нмЗадача.form:ФормаЗадачи` |
| `ChartsOfAccounts.Хозрасчетный` | command:ЖурналПроводок | 8 | `FETCH:ChartsOfAccounts.Хозрасчетный.command:ЖурналПроводок` |
```

Особенности:

- Регистр игнорируется во всех трёх областях.
- Объекты с префиксом `Удалить` пропускаются.
- До 5 номеров строк на файл.
- Поиск по реквизитам работает и по имени, и по типу, и вглубь табличных частей:
  `SEARCH:СправочникСсылка.Контрагенты|attr` найдёт все реквизиты этого типа,
  включая лежащие в ТЧ.
- По достижении `max` обход прекращается немедленно.

#### Примеры

```
SEARCH:ПодобратьДоговор

SEARCH:ПодобратьДоговор|code|in=CommonModules,Documents

SEARCH:арсЗаявка|name

SEARCH:СправочникСсылка.арсРодственники|attr|in=Documents

SEARCH:ПриСозданииНаСервере|code|in=Tasks|max=100
```

#### Цена запроса

`|name` — секунды, файлы не читаются вообще.
`|code` без `in=` на конфигурации в 9 000 объектов — минуты; в пределах
`in=CommonModules` (около 4 000 модулей) — порядка 15 секунд.
`|attr` разбирает XML каждого объекта отбираемых типов.

Поэтому по возможности сразу сужай область через `in=` и режим.

---

### DEPENDENCIES — что с чем связано

Анализ зависимостей объекта в обе стороны плюс граф Mermaid. Нужен перед
правкой объекта: показывает, что он использует и что сломается, если его менять.

```
DEPENDENCIES:Тип.ИмяОбъекта|опция|опция
DEPS:Тип.ИмяОбъекта|опция              — короткий алиас, работает так же
```

#### Опции

| Опция | Что делает |
|---|---|
| `out` | только исходящие — кого использует объект |
| `rev` | только входящие — кто использует объект |
| `all` | оба направления (так же, если направление не указано) |
| `max=N` | лимит входящих связей, по умолчанию 100 |
| `in=Тип,Тип` | сузить обратный обход по типам объектов |

#### Что возвращает

Исходящие (быстро — читаются только модули самого объекта):

- **Вызываемые общие модули** — сверены с реальным списком папок `CommonModules`.
- **Объекты в реквизитах** — из типов, с указанием, через какие реквизиты.
- **Обращения к менеджерам в коде** — `Справочники.X`, `Документы.Y` и другие
  коллекции.

Входящие (обход конфигурации, поэтому есть `in=` и `max=`):

- **Ссылаются в коде** — формы и команды отдельными строками через ключи
  `form:` / `command:`.
- **Ссылаются реквизитами** — включая попадание внутрь составных типов.

В конце — блок `mermaid` с графом: цель подсвечена, рёбра подписаны
(`вызов`, `реквизит`, `менеджер`, `код`), не больше 12 узлов на группу.
Полные списки — в таблицах выше графа.

#### Особенности

- Самоссылки отсекаются: объект не попадает в собственные списки ни через
  стандартный реквизит `Ref`, ни через обращение к своему менеджеру.
- Связь и кодом, и реквизитом даёт одно ребро с подписью `код + реквизит`.
- В исходящем анализе комментарии отсекаются. **В обратном обходе — нет:**
  поиск идёт по файлу целиком, поэтому упоминание цели в чужом комментарии
  попадёт во входящие связи. Учитывай это, оценивая результат.

#### Примеры

```
DEPENDENCIES:Catalogs.Контрагенты

DEPS:Documents.РеализацияТоваровУслуг|out

DEPS:Catalogs.Контрагенты|rev|in=Documents|max=50

DEPS:CommonModules.ОбщегоНазначения|rev|in=Documents,Catalogs
```

#### Цена запроса

`|out` — секунды, читаются только модули самого объекта.
`|rev` — обход конфигурации с разбором XML: на 591 документе порядка
нескольких десятков секунд. Сужай через `in=`.

---

### OUTLINE — оглавление модуля без выгрузки кода

Показывает состав модуля: области, процедуры, функции, маркеры доработок —
без самого кода. Первый шаг для любого крупного модуля.

```
OUTLINE:Тип.ИмяОбъекта
OUTLINE:Тип.ИмяОбъекта.модуль
OUTLINE:Тип.ИмяОбъекта|опция
```

#### Опции

| Опция | Что делает |
|---|---|
| `export` | только экспортные процедуры и функции |
| `marks` | только методы с маркерами доработок `//+` и полный их список |
| `find=подстрока` | только методы, в имени которых есть подстрока |
| `noregion` | только методы, лежащие вне всяких `#Область` |
| `page=N` | страница таблицы, если она не влезла в лимит |
| `all` | без ограничений: все модули объекта и вся таблица целиком |

Область можно указать прямо в адресе — тогда в таблицу попадёт только её
содержимое:

```
OUTLINE:CommonModules.арсОбщийМодульСервер.module#ОбработчикиПодписокНаСобытия
```

#### Что возвращает

Таблицу по каждому модулю. Последняя колонка — готовый запрос
для точечного дозапроса.

```
| Строки | Элемент | Вид | Директива | Параметры | Марк. | Запрос |
|---|---|---|---|---|---|---|
| 1-31 | **#Область ПрограммныйИнтерфейс** | Область |  |  |  | `LINES:CommonModules.X.module:1-31` |
| 3-18 | · `ПолучитьСтатус` | Функция ✓экспорт | &НаСервере | Знач Ссылка | 1 | `PROC:CommonModules.X.module#ПолучитьСтатус` |
| 20-29 | · **#Область Служебные** | Область |  |  |  | `LINES:CommonModules.X.module:20-29` |
| 22-27 | · · `ЗаписатьЛог` | Процедура | &НаКлиенте | Сообщение |  | `PROC:CommonModules.X.module#ЗаписатьЛог` |
```

Особенности:

- Вложенность областей показана отступом `·` — по одному на уровень.
- В колонке «Директива» видны и директивы компиляции (`&НаСервере`),
  и аннотации расширения (`&Вместо("...")`, `&После`, `&ИзменениеИКонтроль`).
- Маркеры `//+Арсансофт ФИО Дата №Задача` считаются по каждой процедуре,
  а под таблицей идёт отдельный список: строка, процедура, текст маркера.
  Это готовая карта доработок модуля.
- Если у объекта больше двух модулей, без указания модуля выводится сводка
  (строк / областей / процедур по каждому) с готовым `OUTLINE:` на каждый.
  Полное оглавление всех модулей — опция `|all`.

#### Ограничения на размер

Оглавление гигантского модуля тоже надо уметь читать, поэтому:

- больше 250 элементов — вместо таблицы выводится **сводка по областям
  верхнего уровня**: строки, число процедур, экспортных и маркеров, готовый
  `OUTLINE:...#ИмяОбласти` на каждую и строка «вне областей»;
- отобранная таблица режется на страницы по 250 строк: внизу видно, какая
  страница показана, и лежит готовый запрос на следующую (`|page=2`);
- список маркеров печатается автоматически только при их числе до 60, иначе
  выводится счётчик и запрос `|marks`; при `|export`, `|find=`, `|noregion`
  и отборе по области маркеры считаются только по отобранным методам,
  а предлагаемый запрос сохраняет текущий отбор;
- в режиме `|marks` таблица методов не печатается — только список маркеров,
  страницами по 300.

В сводке по областям последняя строка — *вне областей*: методы, не попавшие
ни в одну `#Область`. В крупных модулях их бывает большинство (в
`арсОбщийМодульСервер` — 522 из 777), и смотрят их запросом `|noregion`
с постраничным пролистыванием.

Снять любое ограничение — `|all`. Разумный порядок на модуле в 100 000 строк:
`OUTLINE:` (сводка по областям) → `OUTLINE:...#Область`, `|find=` или
`|noregion` → `PROC:` нужного метода.

#### Примеры

```
OUTLINE:CommonModules.арсИнтеграцияThermalConnect

OUTLINE:Documents.РеализацияТоваровУслуг

OUTLINE:Documents.РеализацияТоваровУслуг.object|export

OUTLINE:CommonModules.арсОбщийМодульСервер.module#ОбработчикиПодписокНаСобытия

OUTLINE:CommonModules.арсОбщийМодульСервер|find=Автопостинг

OUTLINE:DataProcessors.ЗагрузкаДанных.form:ОсновнаяФорма|marks
```

#### Цена запроса

Читаются только модули самого объекта — секунды даже на модуле в 20 000 строк.

---

### PROC / REGION / LINES — точечное извлечение кода

Три команды достают из модуля ровно нужный кусок вместо всего файла.

```
PROC:Тип.ИмяОбъекта.ИмяПроцедуры
PROC:Тип.ИмяОбъекта.модуль#ИмяПроцедуры
REGION:Тип.ИмяОбъекта.модуль#ИмяОбласти
LINES:Тип.ИмяОбъекта.модуль:20180-20240
```

Разделители различаются намеренно: `#` отделяет имя внутри модуля,
`:` — диапазон строк. Поэтому ключ формы `form:ФормаДокумента` не путается
с диапазоном: `LINES:Documents.X.form:ФормаДокумента:100-200` разбирается верно.

#### PROC — процедура или функция целиком

Отдаёт метод вместе с шапкой: директивой компиляции, аннотацией расширения
и предшествующими комментариями, включая маркеры `//+Арсансофт`.

Модуль указывать не обязательно — без него просматриваются все модули объекта.
Если одноимённые методы нашлись в нескольких модулях, выдаётся **не первый
попавшийся**, а таблица вариантов с готовыми запросами:

```
| Модуль | Область | Строки | Запрос |
|---|---|---|---|
| `form:ФормаДокумента` | — | 12-48 | `PROC:Documents.Реализация.form:ФормаДокумента#ПриСозданииНаСервере` |
| `form:ФормаСписка` | — | 8-26 | `PROC:Documents.Реализация.form:ФормаСписка#ПриСозданииНаСервере` |
```

Нужны все варианты сразу — опция `|all`.

#### REGION — область целиком

Отдаёт область вместе со строками `#Область` и `#КонецОбласти`; вложенные
области входят внутрь. Модуль обязателен, если нужно попасть в конкретный.

#### LINES — произвольный диапазон

Отдаёт точный диапазон строк — по номерам, полученным из `SEARCH:`
или `OUTLINE:`. Модуль обязателен, если у объекта их несколько (иначе выдаётся
список модулей с готовыми запросами). Конец диапазона за пределами файла
обрезается по последней строке. Если начало диапазона попало внутрь метода,
в шапке будет его имя и готовый `PROC:`, чтобы добрать метод целиком.

#### Шапка фрагмента

У каждого фрагмента шапка «объект / модуль / область / строки»:

```
> Объект: `CommonModules.арсИнтеграцияThermalConnect` / Модуль: `module` (Module)
> / Область: `ПрограммныйИнтерфейс` / Строки: 3-18 из 969
```

По ней сразу видно, куда вставлять правку, и какие номера строк брать
для следующего `LINES:`.

#### Примеры

```
PROC:CommonModules.арсОбщийМодульСервер_Документы.ПолучениеСтатусаДокументаEHS

PROC:Documents.ПоступлениеТоваровУслуг.object#ОбработкаПроведения

PROC:Documents.Реализация.ПриСозданииНаСервере|all

REGION:Documents.ПоступлениеТоваровУслуг.object#ОбработчикиСобытий

LINES:CommonModules.арсОбщийМодульСервер.module:20180-20240
```

#### Цена запроса

Читаются только модули самого объекта. `PROC:` без указания модуля читает
все модули объекта — на документе с десятком форм это доли секунды.

---

### Комбинирование

`FETCH`, `SCHEMA`, `SEARCH`, `DEPENDENCIES`, `OUTLINE`, `PROC`, `REGION`
и `LINES` можно указать в одном поле запроса, каждый с новой строки — результат
попадёт в один файл, имя которого соберётся из типов и объектов запроса.

---

## Карта конфигурации (CONFIG_MAP.md)

Генерируется кнопкой **Выгрузить объекты в MD**.
Содержит все объекты конфигурации с пометками:

| Метка | Значение |
|---|---|
| **М** | есть ManagerModule или ObjectModule |
| **РС** | есть RecordSetModule |
| **Ф** | количество форм |
| **К** | количество команд |

**Если карта приложена к чату** — ИИ использует её для навигации
и формирует FETCH-запросы на основе реальных имён объектов без лишних уточнений.

Карта даёт только *количество* форм и команд. Их **имена** даёт `SCHEMA`,
поэтому маршрут до кода конкретной формы такой:
карта (`Ф=13`) → `SCHEMA` (имена форм) → `FETCH:...form:ИмяФормы` (код).

Если неизвестен даже объект — маршрут начинается с `SEARCH`, который сразу
выдаёт готовые `FETCH:` / `SCHEMA:` в последней колонке.

---

## Правила ИИ при решении задач

### 1. Сначала запрос — потом код
Не пишу реализацию не видя исходников.
Исключение: общие вопросы по платформе, архитектуре, синтаксису BSL.

### 2. Не придумываю имена
Методы, реквизиты, общие модули беру только из кода полученного через FETCH/SCHEMA
или из CONFIG_MAP. Никаких предположений по названиям.

### 3. Запрашиваю зависимости
Если анализируемый метод вызывает другой общий модуль —
запрашиваю и его прежде чем делать выводы или писать код.

### 4. Точно указываю место вставки
Для каждого фрагмента кода указываю:
- тип объекта
- имя объекта
- имя модуля
- имя процедуры (если вставка внутрь существующей)

### 5. Крупные объекты — итеративно
Если у объекта много форм или модуль явно большой:
сначала `SCHEMA` или `OUTLINE`, потом точечный запрос конкретного метода.

### 5.1. Большой модуль не тяну целиком
`FETCH` на модуль в тысячи строк съедает контекст чата, после чего соседний
модуль запросить уже нечем. Порядок такой: `OUTLINE:` — что в модуле есть,
затем `PROC:` (метод целиком) или `LINES:` (нужный диапазон).
`FETCH` оставляю для небольших модулей и случаев, когда действительно нужен
весь текст.

### 6. Второй запрос при необходимости
Если первого FETCH недостаточно для решения задачи —
выдаю второй уточняющий запрос, не додумываю.

### 7. Не знаю, где искать — сначала SEARCH
Если из задачи и карты не ясно, какой объект затронут, выдаю `SEARCH`
с максимально суженной областью, а из его результата беру готовые
`FETCH:` / `SCHEMA:`. Не перебираю объекты наугад.

### 8. Меняю объект — сначала DEPENDENCIES
Перед правкой реквизита, типа или удалением объекта выдаю
`DEPENDENCIES:Тип.Имя|rev`, чтобы увидеть, кого правка затронет.
Не заявляю «это изменение ни на что не влияет», не проверив обратные связи.

---

## Ограничения BSL — держу в голове всегда

```bsl
// ❌ Нельзя — вызов метода прямо на конструкторе
Если (Новый Файл(Путь)).Существует() Тогда ...

// ✅ Правильно — через промежуточную переменную
Ф = Новый Файл(Путь);
Если Ф.Существует() Тогда ...
```

```bsl
// ❌ Нельзя — вложенные процедуры и функции
Функция Внешняя()
    Функция Внутренняя() // синтаксическая ошибка
    КонецФункции
КонецФункции

// ✅ Правильно — все процедуры на одном уровне модуля
```

```bsl
// ❌ Нельзя на клиенте
// Файл, НайтиФайлы, ЧтениеТекста, ЗаписьТекста

// ✅ Только &НаСервере
&НаСервере
Процедура РаботаСФайламиНаСервере()
    Ф = Новый Файл(Путь);
    ...
КонецПроцедуры
```

```bsl
// Транзакции — всегда с обработкой исключения
НачатьТранзакцию();
Попытка
    // ... логика ...
    ЗафиксироватьТранзакцию();
Исключение
    ОтменитьТранзакцию();
    ВызватьИсключение;
КонецПопытки;
```

### Зарезервированные слова в именах переменных

`По` — часть конструкции `Для Сч = 1 По 10 Цикл`, поэтому переменная с именем
`ПО` не компилируется, причём ошибка сыплется каскадом на все последующие
строки, где эта переменная встречается, и настоящая причина теряется.

```bsl
// ❌ Не скомпилируется — По зарезервировано
Для Каждого ПО Из ПапкиОбъектов Цикл

// ✅ Любое незарезервированное имя
Для Каждого ПапкаОб Из ПапкиОбъектов Цикл
```

Тот же риск у имён `Из`, `И`, `Не`, `Или`, `Знач`, `Новый`, `Для`, `Пока`,
`Если`, `Тогда`, `Цикл`, `Возврат`, `Перем`, `Экспорт`.

### Только символы BMP в строковых литералах

1С хранит строки в UCS-2. Символы вне BMP (кодовая точка `U+10000` и выше —
это почти все цветные эмодзи) **молча теряются при сохранении модуля из
Конфигуратора**, ещё до запуска: в исходнике на их месте остаётся пустота.

```bsl
// ❌ Пропадёт при сохранении — суррогатная пара
Сообщить("📝 Реквизит");   // U+1F4DD

// ✅ Переживёт сохранение — символ из BMP
Сообщить("✏ Реквизит");    // U+270F
```

Безопасные значки: `✏ ◆ Σ ⚙ ☰ ⊕ ▣ ▶ ⇄ ◈ ⚒ ✅ ❌ ⚠ ℹ`.
Вариационные селекторы (`U+FE0F` после значка) лучше не использовать.

---

## Как начать работу в новом чате

1. **Приложи карту конфигурации** — если есть, ИИ сразу видит все объекты
2. **Укажи путь к конфигурации** — если он отличается от сохранённого в скрипте
3. **Опиши задачу** — что нужно сделать, какой объект затронут
4. **Жди FETCH/SCHEMA запрос** — ИИ сам попросит нужный код

---

## Быстрая шпаргалка

```
# Получить код общего модуля (CommonModules, WebServices)
FETCH:CommonModules.ИмяМодуля.module

# Получить код модуля менеджера (Catalogs, Documents, Registers...)
FETCH:Catalogs.ИмяСправочника.manager

# Получить код объекта и менеджера справочника
FETCH:Catalogs.ИмяСправочника.manager,object

# Получить форму обработки
FETCH:DataProcessors.ИмяОбработки.form:ИмяФормы

# Получить всё что есть у объекта
FETCH:Documents.ИмяДокумента.all

# Получить структуру (реквизиты, ТЧ, формы, команды) без кода
SCHEMA:Catalogs.ИмяСправочника

# Получить структуру нескольких объектов сразу
SCHEMA:Documents.ИмяДокумента;InformationRegisters.ИмяРегистра

# Узнать имена форм, чтобы затем взять код нужной
SCHEMA:Catalogs.ИмяСправочника
FETCH:Catalogs.ИмяСправочника.form:ФормаЭлемента

# Комбинировать код и структуру
FETCH:CommonModules.ИмяМодуля.manager
SCHEMA:Catalogs.ИмяСправочника

# Найти, где встречается текст (имена + код + реквизиты)
SEARCH:ПодобратьДоговор

# Искать только в коде и только в общих модулях — быстро
SEARCH:ПодобратьДоговор|code|in=CommonModules

# Найти все реквизиты заданного типа
SEARCH:СправочникСсылка.Контрагенты|attr|in=Documents

# Что использует объект (быстро)
DEPS:Documents.ИмяДокумента|out

# Кто пострадает при правке объекта
DEPS:Catalogs.ИмяСправочника|rev|in=Documents

# Что лежит в модуле — без выгрузки кода
OUTLINE:CommonModules.ИмяМодуля

# Только экспортные методы модуля объекта
OUTLINE:Documents.ИмяДокумента.object|export

# Карта доработок модуля по маркерам //+
OUTLINE:CommonModules.ИмяМодуля|marks

# Огромный модуль: сводка по областям, потом одна область
OUTLINE:CommonModules.ИмяМодуля
OUTLINE:CommonModules.ИмяМодуля.module#ИмяОбласти

# Поиск метода по части имени
OUTLINE:CommonModules.ИмяМодуля|find=Автопостинг

# Методы вне областей, вторая страница
OUTLINE:CommonModules.ИмяМодуля|noregion
OUTLINE:CommonModules.ИмяМодуля|noregion|page=2

# Одна процедура целиком, с директивой и комментариями
PROC:CommonModules.ИмяМодуля.ИмяПроцедуры

# Процедура конкретной формы (когда одноимённых много)
PROC:Documents.ИмяДокумента.form:ФормаДокумента#ПриСозданииНаСервере

# Область целиком
REGION:Documents.ИмяДокумента.object#ОбработчикиСобытий

# Точный диапазон строк по номерам из SEARCH или OUTLINE
LINES:CommonModules.ИмяМодуля.module:20180-20240
```
